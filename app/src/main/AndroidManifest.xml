<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.csci3130groupproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewSingleApplicationActivity"></activity>
        <activity android:name=".ViewApplicationsActivity" />
        <activity android:name=".ViewSingleTask" />
        <activity android:name=".ViewTasksActivity" />
        <activity android:name=".EmployeeHomepageActivity" />
        <activity android:name=".SubmitTaskActivity" />
        <activity android:name=".EmployerHomepageActivity" />
        <activity android:name=".EmployeeRegisterActivity" />
        <activity android:name=".EmployerRegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>